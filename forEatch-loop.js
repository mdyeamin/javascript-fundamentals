/* 
The forEatch loop receive a function.
use case-  array.forEatch(function())
 */
// basic forEach structure
const num = [10, 22, 34, 20, 55, 76, 44, 24, 33, 87];

num.forEach(function (num) {
  console.log(num);
});
